#include <bits/stdc++.h>
using namespace std;

int main()
{
    // pair<int, int> p;
    // p = make_pair(10, 20);
    pair<string, int> p;
    p = make_pair("Asif", 20);

    cout << p.first << " " << p.second << "\n";
    return 0;
}